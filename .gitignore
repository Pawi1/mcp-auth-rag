# Development
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.coverage

# Secrets / local config
.env
.env.local
config.json
*.log
*.secret
api.secret

# Local dev scratch (not shipped, not part of the app)
.dev/

# Runtime data (SQLite DB, uploaded files, logs) for a local config.json
# pointing paths.data_root at ./data
/data/

# Binary build artifacts
app/dist/
app/build/
app/*.spec

.claude